import { describe, expect, it } from 'vitest'; import type { ChainRow, Overview } from './api/types'; import { curlExample, filterSortChains, isHttpUrl, qpsFromSnapshots, rpcUrl, statusMeta } from './utils';
describe('dashboard utilities', () => { it('maps status to fixed color and text', () => expect(statusMeta('cooling')).toEqual(['#ec835a','Cooling'])); it('computes incremental qps', () => { const a = { traffic: { ingressTotal: 10 } } as Overview; const b = { traffic: { ingressTotal: 30 } } as Overview; expect(qpsFromSnapshots(a,b,2)).toBe(10) }); it('filters and sorts chains',()=>{const rows=[{chainId:2,name:'Beta',ingressTotal:5},{chainId:1,name:'Alpha',shortName:'eth',ingressTotal:10}] as ChainRow[];expect(filterSortChains(rows,'eth','traffic').map(x=>x.chainId)).toEqual([1]);expect(filterSortChains(rows,'','name').map(x=>x.name)).toEqual(['Alpha','Beta'])}); it('builds developer curl example',()=>{expect(rpcUrl('https://rpc.cryptostack.ai/',42161)).toBe('https://rpc.cryptostack.ai/rpc/42161');const c=curlExample('https://rpc.cryptostack.ai','42161');expect(c).toContain('curl -sS https://rpc.cryptostack.ai/rpc/42161');expect(c).toContain("-H 'content-type: application/json'");expect(c).toContain('{"jsonrpc":"2.0","id":1,"method":"eth_blockNumber","params":[]}')}) });
describe('public URL safety', () => { it('allows only http explorer links', () => { expect(isHttpUrl('https://example.com')).toBe(true); expect(isHttpUrl('javascript:alert(1)')).toBe(false) }) });
