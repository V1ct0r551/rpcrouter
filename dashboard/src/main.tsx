import React from 'react'; import ReactDOM from 'react-dom/client'; import { QueryClient, QueryClientProvider } from '@tanstack/react-query'; import { createBrowserRouter, RouterProvider } from 'react-router-dom'; import { ChainDetailPage, ChainsPage, Layout, OverviewPage, SettingsPage, PublicLayout, PublicHomePage, PublicChainPage } from './components'; import './styles.css'; import './state-warning.css';
const client=new QueryClient({defaultOptions:{queries:{retry:1,refetchOnWindowFocus:false}}});
const router=createBrowserRouter([{path:'/',element:<PublicLayout/>,children:[{index:true,element:<PublicHomePage/>},{path:'chain/:id',element:<PublicChainPage/>}]},{path:'/dashboard',element:<Layout/>,children:[{index:true,element:<OverviewPage/>},{path:'chains',element:<ChainsPage/>},{path:'chains/:id',element:<ChainDetailPage/>},{path:'settings',element:<SettingsPage/>}]}]);
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><QueryClientProvider client={client}><RouterProvider router={router}/></QueryClientProvider></React.StrictMode>);
